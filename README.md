# Japanese → English Subtitle Generator

A local, GPU-accelerated subtitle generator that converts Japanese video audio into English `.srt` subtitles using **faster-whisper** and **Streamlit**.

## ✨ Features
- Japanese → English translation
- GPU acceleration (CUDA)
- Faster-whisper backend
- Handles large videos (1–2+ hours)
- Progress bar with ETA
- Live subtitle preview
- Uncensored output
- Local processing (privacy-friendly)

## 🛠 Requirements
- Python 3.10+
- NVIDIA GPU (recommended)
- CUDA-enabled PyTorch
- FFmpeg (installed separately)

## 📦 Installation

```bash
pip install -r requirements.txt
